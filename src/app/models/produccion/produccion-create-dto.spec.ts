import { ProduccionCreateDto } from './produccion-create-dto';

describe('ProduccionCreateDto', () => {
  it('should create an instance', () => {
    expect(new ProduccionCreateDto()).toBeTruthy();
  });
});
